# http-rdf-Sink
Python based http endpoint for RDF data, that saves the received data in files.

Steps : 

**Build the Docker Image**

```docker-compose build```

**Start the Service**

```docker-compose up -d```

The -d flag runs it in the background.

**Verify It's Running**

```docker ps```
